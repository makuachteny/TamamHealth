{
  "name": "tamamhealth-plt-v2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm --prefix ../platform run setup",
    "dev": "npm --prefix ../platform run dev",
    "build": "npm --prefix ../platform run build",
    "start": "npm --prefix ../platform run start",
    "lint": "npm --prefix ../platform run lint",
    "test": "npm --prefix ../platform run test",
    "test:ci": "npm --prefix ../platform run test:ci"
  }
}
