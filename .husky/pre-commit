#!/usr/bin/env sh
# Catch lint/type errors here (seconds) instead of in CI (~6 minutes).
# Scoped to the packages this commit touches — see .lintstagedrc.mjs.
#
# Emergency bypass:  git commit --no-verify
npx --no-install lint-staged
