'use client';

// Re-export of the shared platform row-actions menu, kept here so existing
// referrals imports keep working. See @/components/RowActionsMenu.
export { default, type RowAction } from '@/components/RowActionsMenu';
